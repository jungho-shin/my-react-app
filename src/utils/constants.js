// Point in time enum
export const PointInTime = {
  OLDEST: 'oldest',
  LATEST: 'latest'
};

// Direction enum
export const Direction = {
  BEFORE: 'before',
  AFTER: 'after'
};

// File type enum
export const FileType = {
  ALIGN: 'align',
  LENS: 'lens',
  LEVELING: 'leveling'
};

